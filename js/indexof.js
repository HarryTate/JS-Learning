const array = ["tiger", "lion", "cat", "dog"];
// indexOf returns the array position of the argument entered
// If it does not exsist -1 is returned
const findLion = array.indexOf("lion");
const findCat = array.indexOf("cat");

console.log(findLion);
console.log(findCat);
